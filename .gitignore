# ---------------------------------------------------------------------------
# 🔴 데이터는 절대 커밋하지 않는다.
# AWES(국립국어원 말평) 배포본과 NIKL 코퍼스는 이용약관 동의 후 받은 자료이고,
# 재배포는 약관 위반이다. 합성 근거도 원문에서 파생됐으므로 같은 취급이다.
# 재현하려는 사람은 각자 받아 README의 스키마대로 data/ 아래에 두면 된다.
# ---------------------------------------------------------------------------
data/
AWES/
*.jsonl
!awes_common/fixtures/*.jsonl

# 학습 산출물 (어댑터·헤드·예측). 용량이 크고 데이터에서 파생된다.
runs/
*.pt
*.safetensors
adapter*/
checkpoint*/

# 모델 가중치·캐시
models/
hfcache/
*.bin

# 로그·임시
*.log
nohup.out
.DS_Store
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
